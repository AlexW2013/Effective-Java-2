package chapter3_common_obj_methods;

/*
    TODO: Review more about clone to become more comfortable with it, then return to this chapter

    If clone is overridden, you should return the object created by invoking super.clone
    General clone rules:
    1. x.clone() != x
    2. x.clone().getClass() == x.getClass()
 */
public class Item11OverrideCloneJudiciously {
}
